# Vite env untuk build PRODUCTION (dibaca otomatis saat `npm run build`).
# VITE_WS_URL dipakai frontend untuk connect ke WebSocket service via Cloudflare Tunnel.
# Ini URL publik (bukan rahasia) — aman di-commit.
VITE_WS_URL=wss://ws-angklunginex.farelfirdaus.site
